
mod math;

fn max(x: i32, y: i32) -> i32 {
    if (x > y) {
        return x;
    }
    else {
        return y;
    }
}

fn min(x: i32, y: i32) -> i32 {
    if (x < y) {
        return x;
    }
    else {
        return y;
    }
}

